meta {
  name: emit retry
  type: http
  seq: 4
}

post {
  url: {{base_url}}/lab/emit
  body: json
}

body:json {
  {
    "scenario": "retry",
    "count": 1,
    "with_callbacks": true,
    "message": "retry from bruno"
  }
}
